// These two lines prevent the file from being included multiple times in the same source file
#ifndef ENUM_HPP
#define ENUM_HPP

enum AnswerType {
    NONE,
    TRUE_NEW,
    FALSE_NEW,
    SKIP,
    TRUE_DISCARD,
    FALSE_DISCARD
};

#endif // ENUM_HPP